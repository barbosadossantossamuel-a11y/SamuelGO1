import AppShell from "@/components/AppShell";
import { trpc } from "@/lib/trpc";
import { categories, templates } from "@/lib/pedidogoData";
import { ArrowRight, Check, Eye, Search, X } from "lucide-react";
import { useMemo, useState } from "react";
import { toast } from "sonner";

export default function Models() {
  const [filter, setFilter] = useState("Todos");
  const [query, setQuery] = useState("");
  const [selected, setSelected] = useState<(typeof templates)[number] | null>(null);
  const updateTheme = trpc.theme.update.useMutation({ onSuccess: ()=>{ toast.success("Visual atualizado", {description:"Seus produtos e informações foram mantidos."}); setSelected(null); }, onError: ()=>toast.error("Não foi possível aplicar o modelo") });
  const list = useMemo(()=>templates.filter(template=>(filter === "Todos" || template.category === filter) && template.name.toLowerCase().includes(query.toLowerCase())), [filter, query]);
  return <AppShell><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-[#ef6b4b]">Aparência</p><h1 className="display-serif mt-2 text-4xl sm:text-5xl">Modelos de cardápio</h1><p className="mt-3 text-sm text-black/50">Comece com uma base pronta e deixe com a cara do seu negócio.</p></div><span className="rounded-full bg-[#e5f1ed] px-3 py-2 text-xs font-bold text-[#3d766a]">12 modelos disponíveis</span></div><div className="mt-8 flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div className="scrollbar-hidden flex gap-2 overflow-x-auto pb-1">{categories.map(category=><button key={category} onClick={()=>setFilter(category)} className={`whitespace-nowrap rounded-full px-4 py-2 text-xs font-bold transition ${filter===category?"bg-[#111315] text-white":"bg-white text-black/45 hover:text-black"}`}>{category}</button>)}</div><div className="relative shrink-0"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-black/30" /><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Pesquisar modelo..." className="h-10 w-full rounded-full border border-black/10 bg-white pl-9 pr-4 text-xs outline-none focus:border-[#ef6b4b] sm:w-60" /></div></div><div className="mt-8 grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{list.map(template=><article key={template.id} className="group overflow-hidden rounded-3xl bg-white soft-shadow"><div className="relative h-52 overflow-hidden"><img src={template.image} alt={template.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent" />{template.badge&&<span className="absolute left-4 top-4 rounded-full bg-[#f1bd69] px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-[#111315]">{template.badge}</span>}<span className="absolute bottom-4 left-4 text-sm font-bold text-white">{template.category}</span></div><div className="p-5"><div className="flex items-start justify-between"><h2 className="text-lg font-bold">{template.name}</h2><div className="flex gap-1 pt-1">{template.colors.map(color=><span key={color} style={{backgroundColor:color}} className="h-3 w-3 rounded-full border border-black/10" />)}</div></div><p className="mt-2 min-h-10 text-sm leading-relaxed text-black/50">{template.description}</p><div className="mt-5 flex gap-2"><button onClick={()=>setSelected(template)} className="flex flex-1 items-center justify-center rounded-full border border-black/10 py-3 text-xs font-bold"><Eye className="mr-2 h-4 w-4" /> Visualizar</button><button onClick={()=>setSelected(template)} className="flex flex-1 items-center justify-center rounded-full bg-[#ef6b4b] py-3 text-xs font-bold text-white">Usar modelo <ArrowRight className="ml-2 h-4 w-4" /></button></div></div></article>)}</div>{!list.length&&<div className="rounded-3xl bg-white py-16 text-center"><Search className="mx-auto h-8 w-8 text-black/15" /><p className="mt-3 font-bold">Nenhum modelo encontrado</p><p className="mt-1 text-sm text-black/45">Tente outro termo ou categoria.</p></div>}
    {selected&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-[#111315]/60 p-4 backdrop-blur-sm"><div className="max-h-[90vh] w-full max-w-3xl overflow-y-auto rounded-[28px] bg-[#f6f4ef] shadow-2xl"><div className="relative h-56 sm:h-72"><img src={selected.image} alt={selected.name} className="h-full w-full object-cover" /><div className="absolute inset-0 bg-gradient-to-t from-black/70 via-black/10 to-transparent" /><button onClick={()=>setSelected(null)} className="absolute right-4 top-4 rounded-full bg-black/45 p-2 text-white"><X className="h-5 w-5" /></button><div className="absolute bottom-6 left-6"><p className="text-sm text-white/65">{selected.category}</p><h2 className="display-serif mt-1 text-4xl text-white">{selected.name}</h2></div></div><div className="p-6 sm:p-8"><div className="grid gap-8 sm:grid-cols-[1fr_auto] sm:items-end"><div><p className="text-sm leading-relaxed text-black/55">{selected.description} O modelo já vem com estrutura de categorias, vitrine de produtos, carrinho e checkout para você editar.</p><div className="mt-5 grid gap-3 sm:grid-cols-2">{["Layout mobile-first", "Categorias organizadas", "Carrinho e checkout", "Visual personalizável"].map(item=><p key={item} className="flex items-center gap-2 text-sm font-semibold"><span className="flex h-5 w-5 items-center justify-center rounded-full bg-[#e5f1ed] text-[#3d766a]"><Check className="h-3 w-3" /></span>{item}</p>)}</div></div><button disabled={updateTheme.isPending} onClick={()=>updateTheme.mutate({templateId:selected.id,primaryColor:selected.accent,secondaryColor:selected.colors[0],backgroundColor:selected.colors[2]})} className="button-pop rounded-full bg-[#ef6b4b] px-6 py-4 text-sm font-bold text-white">{updateTheme.isPending?"Aplicando…":"Usar este modelo"} <ArrowRight className="ml-1 inline h-4 w-4" /></button></div><p className="mt-7 rounded-2xl bg-[#e8eee9] p-4 text-xs leading-relaxed text-[#315c53]"><b>Seus produtos e informações serão mantidos.</b> Apenas o visual do cardápio será alterado.</p></div></div></div>}
  </AppShell>;
}
